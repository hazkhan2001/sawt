# Copy to .env and fill in. .env is gitignored; this file is not.
# Only needed to RUN THE HARVESTER. The web app needs none of these.

# Freesound API. The client id is public, the secret is not.
# https://freesound.org/apiv2/apply/
FREESOUND_TOKEN=
FREESOUND_CLIENT_ID=
FREESOUND_CLIENT_SECRET=
